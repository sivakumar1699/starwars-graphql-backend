import type { DirectiveNode } from "graphql";
import type { DefinitionCollection } from "../../schema-model/parser/definition-collection";
export declare function getUserDefinedDirectives(definitionCollection: DefinitionCollection): {
    userDefinedFieldDirectivesForNode: Map<string, Map<string, DirectiveNode[]>>;
    userDefinedDirectivesForNode: Map<string, DirectiveNode[]>;
    propagatedDirectivesForNode: Map<string, DirectiveNode[]>;
    userDefinedDirectivesForInterface: Map<string, DirectiveNode[]>;
    userDefinedDirectivesForUnion: Map<string, DirectiveNode[]>;
};
//# sourceMappingURL=user-defined-directives.d.ts.map